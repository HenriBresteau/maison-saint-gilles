import LayoutPage from '@/app/components/layout/LayoutPage';

const page = ({}) => {
  return (
    <LayoutPage>
      <div className="flex">From page CONTACT</div>
    </LayoutPage>
  );
};

export default page;
